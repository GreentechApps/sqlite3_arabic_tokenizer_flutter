name: sqlite3_arabic_tokenizer_flutter
description: "SQLite3 Arabic Tokenizer"
version: 0.0.2
homepage: https://github.com/GreentechApps/sqlite3_arabic_tokenizer_flutter

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: org.gtaf.sqlite3_arabic_tokenizer_flutter
        pluginClass: Sqlite3ArabicTokenizerFlutterPlugin
      ios:
        pluginClass: Sqlite3ArabicTokenizerFlutterPlugin
#      linux:
#        pluginClass: Sqlite3ArabicTokenizerFlutterPlugin
      macos:
        pluginClass: Sqlite3ArabicTokenizerFlutterPlugin
      windows:
        pluginClass: Sqlite3ArabicTokenizerFlutterPluginCApi

  assets:
    - assets/sqlite3-arabic-tokenizer.dll


